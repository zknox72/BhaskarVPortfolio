---
import { getCollection } from 'astro:content';
// Component Imports
import ContactCTA from '../components/ContactCTA.astro';
import Hero from '../components/Hero.astro';
import Icon from '../components/Icon.astro';
import Pill from '../components/Pill.astro';
import { getImagePath } from '../utils/imageHelper';
// Layout import — provides basic page elements: <head>, <nav>, <footer> etc.
import BaseLayout from '../layouts/BaseLayout.astro';

// Full Astro Component Syntax:
// https://docs.astro.build/basics/astro-components/
---

<BaseLayout>
	<div class="stack gap-20 lg:gap-48">
		<div class="wrapper stack gap-8 lg:gap-20">
			<header class="hero">
				<Hero
					title="Bhaskar Vajipeyajula"
					tagline="Mechanical Engineer & Researcher focused on viscoelastic modeling, additive manufacturing, and fracture mechanics. Instructional Assistant Professor at Texas A&M University."
					align="start"
				>
					<div class="roles">
						<Pill><Icon icon="code" size="1.33em" /> Researcher</Pill>
						<Pill><Icon icon="microphone-stage" size="1.33em" /> Educator</Pill>
						<Pill><Icon icon="pencil-line" size="1.33em" /> Engineer</Pill>
					</div>
					<button class="resume-download" onclick="downloadResume()">
						<Pill><Icon icon="arrow-right" size="1.33em" /> Download Resume </Pill>
					</button>
				</Hero>

				<img
					alt="Bhaskar Vajipeyajula"
					width="480"
					height="620"
					src={getImagePath('/assets/portrait.jpg')}
				/>
			</header>

			<section class="research-section">
				<div class="section-header-with-link">
					<h2>Research Areas</h2>
					<a href="research" class="view-all-link">View All Research →</a>
				</div>
				<div class="research-grid">
					<div class="research-card">
						<h3>Viscoelastic Modeling</h3>
						<p>Developing thermodynamic models for asphalt mixtures and nonlinear viscoelasticity frameworks.</p>
					</div>
					<div class="research-card">
						<h3>Additive Manufacturing</h3>
						<p>Expertise in FFF/3D printing, friction stir additive manufacturing, and process optimization.</p>
					</div>
					<div class="research-card">
						<h3>Fracture Mechanics</h3>
						<p>Analysis of fracture toughness in thermoplastics and stress concentrations in porous materials.</p>
					</div>
				</div>
			</section>

			<section class="publications-section">
				<div class="section-header-with-link">
					<h2>Publications & Journals</h2>
					<a href="publications" class="view-all-link">View All Publications →</a>
				</div>
				<p>Research published in leading engineering journals including work on additive manufacturing, fracture mechanics, and viscoelasticity.</p>
				<div class="publications-list">
					<div class="publication-item">
						<h4>Process-Induced Shrinking and Warping in Additively Manufactured Polycarbonate Plates</h4>
						<p>Applications in Engineering Science, 2025</p>
					</div>
					<div class="publication-item">
						<h4>Effect of Specimen Size, Layout, and Bead Width on the Linear Elastic Fracture Toughness of FFF-Processed Polylactide</h4>
						<p>Engineering Fracture Mechanics, vol. 315, 2025</p>
					</div>
					<div class="publication-item">
						<h4>A Thermodynamic Viscoelastic Model to Capture the Effects of Confinement Pressure on Asphalt Mixtures</h4>
						<p>International Journal of Pavement Engineering, vol. 25, 2024</p>
					</div>
					<div class="publication-item">
						<h4>Stress Concentration Due to the Presence of a Rigid Elliptical Inclusion in Porous Elastic Solids</h4>
						<p>International Journal of Elasticity, 2023</p>
					</div>
					<div class="publication-item">
						<h4>Dynamics of a Single Buoyant Plume in FENE-P Fluid</h4>
						<p>Physics of Fluids, vol. 29, 2017</p>
					</div>
				</div>
			</section>
		</div>
	</div>

	<ContactCTA />
	</div>
</BaseLayout>

<style>
	.hero {
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 2rem;
	}

	.roles {
		display: none;
	}

	.resume-download {
		display: none;
		padding: 0.75rem 1.5rem;
		gap: 0.6rem;
		color: var(--accent-bg);
		border: 1px solid rgba(var(--accent-color), 0.6);
		background: linear-gradient(135deg, rgba(var(--accent-color), 0.15) 0%, rgba(var(--accent-color), 0.05) 100%);
		border-radius: 999rem;
		font-size: var(--text-md);
		line-height: 1.35;
		white-space: nowrap;
		cursor: pointer;
		font-weight: 600;
		transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
		align-items: center;
		gap: 0.5rem;
		font-family: inherit;
		margin-top: 0.5rem;
	}

	.resume-download:hover {
		background: linear-gradient(135deg, rgba(var(--accent-color), 0.25) 0%, rgba(var(--accent-color), 0.12) 100%);
		border-color: rgba(var(--accent-color), 0.8);
		box-shadow: 0 4px 12px rgba(var(--accent-color), 0.2);
		transform: translateY(-2px);
	}

	.resume-download:active {
		transform: translateY(0);
	}

	.hero img {
		aspect-ratio: 5 / 4;
		width: 100%;
		max-width: 280px;
		object-fit: cover;
		object-position: top;
		border-radius: 1.5rem;
		box-shadow: var(--shadow-md);
	}

	@media (min-width: 50em) {
		.hero {
			display: grid;
			grid-template-columns: 6fr 4fr;
			padding-inline: 2.5rem;
			gap: 3.75rem;
		}

		.roles {
			margin-top: 0.5rem;
			display: flex;
			gap: 0.5rem;
		}

		.resume-download {
			display: inline-flex;
		}

		.hero img {
			aspect-ratio: 3 / 4;
			width: 100%;
			max-width: 320px;
			border-radius: 4.5rem;
			object-fit: cover;
		}
	}

	/* ====================================================== */

	.section {
		display: grid;
		gap: 2rem;
	}

	.with-background {
		position: relative;
	}

	.with-background::before {
		--hero-bg: var(--bg-image-subtle-2);

		content: '';
		position: absolute;
		pointer-events: none;
		left: 50%;
		width: 100vw;
		aspect-ratio: calc(2.25 / var(--bg-scale));
		top: 0;
		transform: translateY(-75%) translateX(-50%);
		background:
			url('/assets/backgrounds/noise.png') top center/220px repeat,
			var(--hero-bg) center center / var(--bg-gradient-size) no-repeat,
			var(--gray-999);
		background-blend-mode: overlay, normal, normal, normal;
		mix-blend-mode: var(--bg-blend-mode);
		z-index: -1;
	}

	.with-background.bg-variant::before {
		--hero-bg: var(--bg-image-subtle-1);
	}

	.section-header {
		justify-self: center;
		text-align: center;
		max-width: 50ch;
		font-size: var(--text-md);
		color: var(--gray-300);
	}

	.section-header h3 {
		font-size: var(--text-2xl);
	}

	@media (min-width: 50em) {
		.section {
			grid-template-columns: repeat(4, 1fr);
			grid-template-areas: 'header header header header' 'gallery gallery gallery gallery';
			gap: 5rem;
		}

		.section.with-cta {
			grid-template-areas: 'header header header cta' 'gallery gallery gallery gallery';
		}

		.section-header {
			grid-area: header;
			font-size: var(--text-lg);
		}

		.section-header h3 {
			font-size: var(--text-4xl);
		}

		.with-cta .section-header {
			justify-self: flex-start;
			text-align: left;
		}

		.gallery {
			grid-area: gallery;
		}

		.cta {
			grid-area: cta;
		}
	}

	/* ====================================================== */

	.research-section,
	.publications-section {
		margin-top: 0;
		padding: 3rem 0;
		width: 100%;
	}

	.section-header-with-link {
		display: flex;
		justify-content: space-between;
		align-items: center;
		gap: 2rem;
		flex-wrap: wrap;
		margin-bottom: 2rem;
	}

	.research-section h2,
	.publications-section h2 {
		font-size: var(--text-3xl);
		margin: 0;
		color: var(--gray-100);
	}

	.view-all-link {
		color: var(--accent-regular);
		text-decoration: none;
		font-size: var(--text-md);
		font-weight: 600;
		transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
		white-space: nowrap;
		display: inline-flex;
		align-items: center;
		gap: 0.5rem;
	}

	.view-all-link:hover {
		color: var(--accent-bg);
		text-decoration: underline;
		text-underline-offset: 0.3em;
		transform: translateX(4px);
	}

	.research-grid {
		display: grid;
		grid-template-columns: 1fr;
		gap: 2rem;
		margin-bottom: 0;
	}

	@media (min-width: 50em) {
		.research-grid {
			grid-template-columns: repeat(3, 1fr);
		}
	}

	.research-card {
		display: flex;
		flex-direction: column;
		padding: 2rem;
		border: 1px solid var(--gray-800);
		border-radius: 1.5rem;
		background: var(--gradient-subtle);
		box-shadow: var(--shadow-sm);
		transition: all 0.3s ease;
	}

	.research-card:hover {
		border-color: var(--gray-700);
		box-shadow: var(--shadow-md);
		background: var(--gradient-subtle-2);
	}

	.research-card h3 {
		font-size: var(--text-lg);
		margin-bottom: 1rem;
		color: var(--gray-100);
	}

	.research-card p {
		color: var(--gray-300);
		font-size: var(--text-sm);
		line-height: 1.5;
	}

	.publications-section p {
		color: var(--gray-300);
		margin-bottom: 1.5rem;
		line-height: 1.6;
		font-size: var(--text-md);
	}

	.publications-list {
		display: grid;
		gap: 2rem;
		grid-template-columns: 1fr;
		width: 100%;
		margin: 0 auto;
	}

	@media (min-width: 640px) {
		.publications-list {
			grid-template-columns: repeat(2, 1fr);
			gap: 2rem;
		}
	}

	@media (min-width: 1024px) {
		.publications-list {
			grid-template-columns: repeat(3, 1fr);
			gap: 2rem;
		}
	}

	.publication-item {
		padding: 2rem;
		border: 1px solid var(--gray-800);
		color: var(--gray-200);
		background: var(--gradient-subtle);
		border-radius: 1.5rem;
		transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
		display: flex;
		flex-direction: column;
		gap: 0.75rem;
		box-shadow: var(--shadow-sm);
		overflow: hidden;
		position: relative;
	}

	.publication-item::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		height: 0;
		background: linear-gradient(90deg, var(--accent-regular), rgba(var(--accent-color), 0.3), transparent);
		transition: height 0.3s ease;
	}

	.publication-item h4 {
		font-size: var(--text-md);
		margin: 0;
		color: var(--gray-100);
		font-weight: 700;
		line-height: 1.6;
		position: relative;
		z-index: 1;
	}

	.publication-item p {
		margin: 0;
		font-size: var(--text-sm);
		color: var(--gray-400);
		font-style: italic;
		position: relative;
		z-index: 1;
	}

	.publication-item:hover {
		background: var(--gradient-subtle-2);
		border-color: var(--gray-700);
		box-shadow: var(--shadow-md);
		transform: translateY(-2px);
	}

	.publication-item:hover::before {
		height: 3px;
	}
</style>

<script>
	function downloadResume() {
		const link = document.createElement('a');
		link.href = '/assets/cv.pdf';
		link.download = 'Bhaskar_Vajipeyajula_Resume.pdf';
		document.body.appendChild(link);
		link.click();
		document.body.removeChild(link);
	}
</script>
